<tr>
  <td><%= question.title %></td>
  <td><%= question.content %></td>
  <td>
   <%= link_to t(".delete"), user_question_path(question), method: :delete, data: { confirm: 'Are you sure?' } %>
   <%= link_to t(".edit"), edit_user_question_path(question) %>
 </td>
</tr>
